-- Аватарку теперь выбирают из готового набора, а не загружают файлом: в
-- колонке лежит идентификатор картинки, а не адрес. Прежние значения вели на
-- файлы локального хранилища, которого больше нет, поэтому колонка заводится
-- заново вместо переименования.

-- AlterTable
ALTER TABLE "User" DROP COLUMN "avatarUrl",
ADD COLUMN     "avatar" TEXT;

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /**
   * Корневой сертификат облачной базы нужен в рантайме: строка подключения
   * ссылается на него через `sslrootcert`, и без проверки сертификата
   * соединение с базой открыто для подмены сервера.
   *
   * Путь к файлу приходит из переменной окружения, поэтому сборщик не видит
   * зависимости и не кладёт файл рядом с функцией сам — его приходится
   * называть явно.
   */
  outputFileTracingIncludes: {
    "/**": ["./certs/timeweb-ca.crt"],
  },
};

export default nextConfig;

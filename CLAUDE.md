# Triply — соглашения проекта

Приложение для совместных поездок: учёт общих расходов и расчёт, кто кому
сколько должен перевести.

## Стек

Next.js 16 (App Router, Turbopack) + TypeScript strict, PostgreSQL через
Prisma 7, Tailwind v4, своя авторизация на сессиях в БД, Vitest для расчётного
ядра.

**Prisma 7.** Строка подключения живёт в `prisma.config.ts`, а не в схеме, и
клиент работает через driver adapter (`@prisma/adapter-pg`). `.env` Prisma сама
больше не подхватывает — он загружается в конфиге явно.

**UI-примитивы свои.** `src/components/ui/` — небольшой набор компонентов на
Tailwind: кнопки, поля, карточки, аватарки, подтверждение действия. Переменные
цветов названы по соглашению shadcn/ui, поэтому компоненты оттуда можно
добавлять по одному без переделки палитры. Выпадающие списки и чекбоксы —
нативные: на телефоне системный выбор удобнее любого самописного.

## Правила, которые важно соблюдать

**Деньги — только целые минорные единицы.** Никаких `float` в суммах. Всё, что
касается форматирования и разбора ввода, живёт в `src/lib/money.ts`. У валюты
может быть 0 знаков после запятой (иена, донг) — не хардкодить деление на 100.

**Расчётное ядро не знает про базу и React.** `src/lib/settlement/` — чистые
функции. Любая новая логика расчёта пишется там и покрывается тестом, а не
внутри компонента или обработчика запроса.

**Доли расхода всегда материализованы.** `ExpenseShare` хранит конкретные суммы
даже при делении поровну. Сумма долей обязана совпадать с суммой расхода —
это проверяется и на клиенте, и на сервере перед записью.

**Расходы ссылаются на `TripMember`, а не на `User`.** Гость без аккаунта и
зарегистрированный участник для расчёта неразличимы. Привязка гостя к аккаунту
— это установка `userId` у существующей строки `TripMember`.

**Секрет из письма живёт в базе только хешем.** Токен сессии, ссылка
восстановления пароля и ссылка подтверждения новой почты хранятся как SHA-256
(`src/lib/auth/token.ts`): дамп базы не должен давать вход. Сроки и паузы — в
`src/lib/auth/reset-link.ts` и `email-change-link.ts`, отдельно от запросов,
потому что это политика и она покрыта тестом. Там же рядом
`login-throttle.ts` — пауза после неудачных входов, по тем же правилам:
политика с тестом отдельно, работа с базой отдельно.

**Приглашение в поездку — исключение из правила о хешах.** Токен ссылки
(`TripInvite`) лежит в базе открытым: ссылку надо уметь показать ещё раз, а
«кинь заново» случается чаще, чем утечка дампа. Цена мала — ссылка открывает
одну поездку, данные которой в том же дампе, и входа в аккаунт не даёт. Ссылка
на поездку одна, отзыв — это удаление строки, срока жизни нет: работает, пока
поездка активна. Что ссылка делает с открывшим, решает `inviteOutcome` в
`src/lib/trips/invite-link.ts` — одна функция на страницу и на действие, иначе
кнопка обещала бы то, на что действие отвечает отказом.

**Смена почты подтверждается письмом.** Новый адрес ждёт в
`EmailChangeRequest` и становится входом только после перехода по ссылке —
пароль отвечает за «это хозяин аккаунта», письмо за «адрес существует и он не с
опечаткой». Смена пароля отменяет начатую заявку.

**Почта своя, без зависимостей.** `src/lib/mail/` — сборка MIME и небольшой
SMTP-клиент. Тексты писем лежат в `templates.ts` рядом друг с другом, как и
строки интерфейса. Без `MAIL_SMTP_HOST` письмо печатается в консоль — в
разработке этого достаточно, в продакшене это ошибка.

**Права проверяются на сервере.** Скрыть кнопку в интерфейсе недостаточно:
каждый мутирующий запрос сам проверяет, что пользователь состоит в поездке и
что поездка не завершена. Новое действие — новый тест рядом
(`src/lib/actions/*.test.ts`): и на отказ постороннему, и на то, что после
отказа в базе ничего не изменилось.

**Тесты действий идут в настоящую базу.** Обвязка в `src/test/`: `triply_test`
заводится и мигрирует сама, таблицы чистятся перед каждым тестом, данные
готовят фабрики, а не другие действия. Подменять клиент Prisma нельзя — проверка
прав и есть запрос, мок проверял бы сам себя. Подменяется ровно то, чего вне
запроса Next не существует: cookie, `redirect`, `revalidatePath`. Вход — через
настоящую сессию (`signIn`), поэтому «не вошёл» и «вошёл не тем» — разные
случаи, как в жизни.

**Выкат — Vercel Hobby, база в Timeweb Cloud.** Сборку описывает `vercel.json`:
она же накатывает миграции, поэтому миграция обязана быть переносима старым
кодом — схема меняется раньше, чем код становится живым.

База платная и маленькая: 25 соединений на всех. Пул `pg` поэтому ограничен
явно в `src/lib/db.ts` — экземпляров функции Vercel поднимается сколько угодно,
и каждый держит свой пул. Пулера соединений у провайдера нет, так что миграции
идут по обычной строке, а `DIRECT_DATABASE_URL` нужен только если база однажды
окажется за пулером.

Сертификат базы самоподписанный и лежит в репозитории (`certs/timeweb-ca.crt`),
а строка подключения ссылается на него через `sslrootcert`. В коде задавать
`ssl` бесполезно: `pg` отдаёт приоритет разобранной строке подключения и молча
перетирает поле. Подробности — в `docs/deploy.md`.

## Модель прав

- Любой участник поездки: редактирует поездку, расходы, состав участников,
  завершает и переоткрывает поездку.
- Только создатель поездки: удаляет поездку.
- Добавить в поездку можно только своего подтверждённого друга — или кого
  угодно ссылкой-приглашением: обладание ссылкой и есть согласие.
- Ссылкой распоряжается любой участник: выпускает, показывает, отзывает.
- Участника, который платил или числится в расходах, удалить нельзя.
- Завершённая поездка закрыта на запись, но отметки о переводах работают —
  ради них её чаще всего и открывают.

## Дизайн

Палитра — хвойный `#123C35`, закатный оранжевый `#ff6b35`, кремовый фон
`#f7f4ec`, sage `#a8c4b5`, текст `#17201e`. Шрифт Manrope. Mobile-first: узкий
экран основной.

Оранжевый — акцент, а не фон: одно акцентное действие на экран плюс суммы,
требующие внимания. Если им залить половину страницы, он перестанет работать.

## Язык

Интерфейс русский. Строки не размазываем по компонентам без нужды — так позже
можно будет добавить второй язык.
